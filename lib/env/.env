# Install flutter_dotenv
flutter pub add flutter_dotenv

# Create .env file in project root
cat > .env << 'EOF'
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=eyJyour-anon-key-here
EOF

# Add .env to .gitignore
echo ".env" >> .gitignore